<html>
    <head>
        <title><%= title %></title>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
        <meta name="description" content="">
        <meta name="author" content="">
        <link rel="stylesheet" href="bootstrap/css/bootstrap.min.css">
        <link rel="stylesheet" href="stylesheets/webconsole.css">
    </head>
    <body>
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">BaseballSim</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li class="active"><a href="#">Home</a></li>
                        <li><a href="#about">About</a></li>
                    </ul>
                </div><!--/.nav-collapse -->
            </div>
        </nav>

        <div class="container" ng-app="app" ng-controller="ConsoleController">
            <div class="inner-container">
                <div ng-class="{selected: showPlayerList}" class="display-box" ng-click="showPlayerList = !showPlayerList; loadPlayers()">Players</div>
                <div ng-class="{selected: showUserList}" class="display-box" ng-click="showUserList = !showUserList; loadUsers()">Users</div>
                <div ng-class="{selected: shiftKeyDown}" class="display-box">Teams</div>
                <div ng-class="{selected: shiftKeyDown}" class="display-box">Teams</div>

                <div class="display-rect"  ng-if="showPlayerList">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>Players</th>
                        </tr>
                        </thead>
                        <tr ng-repeat="player in players">
                            <td>{{player}}</td>
                            <td><button class="btn btn-danger" ng-click="deletePlayer(player)">X</button></td>
                        </tr>
                        <tr>
                            <button class="btn btn-success" ng-click="createPlayer()">Add Player</button>
                        </tr>
                    </table>
                </div>

                <div class="display-rect"  ng-if="showUserList">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>Users</th>
                        </tr>
                        </thead>
                        <tr ng-repeat="user in users">
                            <td>{{user}}</td>
                            <td><button class="btn btn-danger" ng-click="deleteUser(player)">X</button></td>
                        </tr>
                        <tr>
                            <button class="btn btn-success" ng-click="createUser()">Add User</button>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
    </body>

    <script src="jquery/jquery.min.js"></script>
    <script src="bootstrap/js/bootstrap.min.js"></script>
    <script src="angular/angular.min.js"></script>
    <script src="javascripts/app.js"></script>
    <script src="javascripts/service/UserService.js"></script>
    <script src="javascripts/service/PlayerService.js"></script>
    <script src="javascripts/controller/ConsoleController.js"></script>
</html>
